export type Tour = {
  city: string;
  country: string;
  title: string;
  description: string;
  stops: string[];
};
export type Destination = {
  city: string;
  country: string;
};

export type searchTerm = string | undefined;
